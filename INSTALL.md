This project has no dependencies, so nothing must be done for installation.
Everything can be run in the typical format from the project root `.` with:

- Running driver code: `dune exec bin/main.exe`
- Running tests: `dune test`
